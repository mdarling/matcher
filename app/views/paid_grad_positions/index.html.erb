<h1>Listing paid_grad_positions</h1>

<table>
  <tr>
    <th>Unmjobs</th>
    <th>Postingnumber</th>
    <th>Positiontitle</th>
    <th>Workperiod</th>
    <th>Weeklytimecommitment</th>
    <th>Deliverables</th>
    <th>Desiredskills</th>
    <th>Desiredclasses</th>
    <th>Recordbegin</th>
    <th>Recordend</th>
    <th>Minimumgpa</th>
    <th>Academiclevel</th>
    <th>Jobdescription</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @paid_grad_positions.each do |paid_grad_position| %>
  <tr>
    <td><%= paid_grad_position.unmJobs %></td>
    <td><%= paid_grad_position.postingNumber %></td>
    <td><%= paid_grad_position.positionTitle %></td>
    <td><%= paid_grad_position.workPeriod %></td>
    <td><%= paid_grad_position.weeklyTimeCommitment %></td>
    <td><%= paid_grad_position.deliverables %></td>
    <td><%= paid_grad_position.desiredSkills %></td>
    <td><%= paid_grad_position.desiredClasses %></td>
    <td><%= paid_grad_position.recordBegin %></td>
    <td><%= paid_grad_position.recordEnd %></td>
    <td><%= paid_grad_position.minimumGPA %></td>
    <td><%= paid_grad_position.academicLevel %></td>
    <td><%= paid_grad_position.jobDescription %></td>
    <td><%= link_to 'Show', paid_grad_position %></td>
    <td><%= link_to 'Edit', edit_paid_grad_position_path(paid_grad_position) %></td>
    <td><%= link_to 'Destroy', paid_grad_position, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Paid grad position', new_paid_grad_position_path %>
