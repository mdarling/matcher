<h1>Listing projects</h1>
	<%= search_form_for @search do |f| %>
		<%= f.select :department_cont, options_from_collection_for_select(Department.all, "name", "name", 			@search.department) %>
	<div class="actions">
		<%= f.submit "Search" %>
	</div>
	<% end %>


<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @project_surveys.each do |project| %>
	<% if project.unpaid_undergrads_needed == "1" || 
				project.paid_undergrads_needed == "1" %>
		<tr>
		  <td><%= link_to 'Show', project %></td>
		  <td><%= link_to 'Edit', edit_project_path(project) %></td>
		  <td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
		</tr>
  <% end %>
<% end %>
</table>

<br />

<%= link_to 'New Project', new_project_path %>
